sudo: required

services:
  - docker

  before_install:
    - docker build -t renatgatin/todoapp-api-docker .

  script:
    - echo "add tests here"

  after_success:
    - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
    - docker push renatgatin/todoapp-api-docker

